# Калькулятор НДС

Простой одностраничный калькулятор для расчёта налогов (НДС + КПН) для Казахстана.

## Ссылка

- Сайт: https://me-qr.kz/nds/
- Репозиторий: https://github.com/mr-diamant/nds-calculator

## Как работает

1. Пользователь вводит "чистую сумму" — сумму, которую хочет получить на руки.
2. Калькулятор рассчитывает сумму с налогом, умножая чистую сумму на коэффициент `1.312217`.
3. От полученной суммы с налогом вычисляются:
   - НДС — 16%
   - КПН — 10%

## Формулы

### Режим по умолчанию — КПН от суммы с налогом

```
total = clean / (1 - 0.16 - 0.10)
nds   = total × 0.16
kpn   = total × 0.10
```

### Режим "КПН за минусом налогов"

```
total = clean / (1 - 0.16 - 0.10 × (1 - 0.16))
nds   = total × 0.16
kpn   = (total - nds) × 0.10
```

Переключатель находится на странице под полем ввода суммы.

## Файлы

- `index.html` — весь сайт (HTML + CSS + JS).

## Локальный запуск

Просто откройте файл `index.html` в браузере. Для разработки удобно использовать любой локальный сервер, например:

```bash
python3 -m http.server 8000
```

После этого откройте http://localhost:8000.

## Деплой

Сайт деплоится на хостинг `me-qr.kz/nds/` вручную или через GitHub + FTP.

## История изменений

- Два режима: быстрый расчёт сверху и полный расчёт с закупами внизу. При добавлении товара верхний блок скрывается.
- КПН в полном режиме считается от чистой прибыли с учётом закупов.
- Добавлена строка "Чистая прибыль" в итоговый блок.
- Переключатель "Чистый КПН" появляется под кнопкой "Рассчитать" после первого расчёта.
- Упрощена форма товара: одна галочка "С учётом НДС", НДС считается автоматом 16%.
- Один товар = один закуп, кнопка переименована в "Добавить товар".
- Убран лишний divider в итоговом блоке.
- Создан базовый калькулятор налогов.
